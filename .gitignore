node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite

.vercel
tsconfig.tsbuildinfo

.vercel

# Supabase CLI artefacts
supabase/.temp/
supabase/.branches/

# direnv — per-project CLI tokens (never commit)
.envrc

# Claude Code — local project context (never commit)
CLAUDE.local.md

# Claude Code — locally-installed skills (e.g. ui-ux-pro-max)
.claude/skills/

# Claude Code — MCP server config (contains API keys, never commit)
.mcp.json
